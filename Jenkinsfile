pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "==========================start build jar!======================="
                sh 'ls -al'
                sh 'pwd'
//                 sh './mvn clean package'
            }
        }
    }
}